openapi: 3.1.0
info:
  title: Event API
  version: 1.0.0
paths:
  /event:
    get:
      tags:
        - Event
      operationId: list
      responses:
        200:
          description: List is returned
          content:
            application/json:
              schema:
                $ref: '../models/event.yaml#/components/schemas/Event'
